# 配置体系

配置分为 `map`、`view`、`basemaps`、`layers`、`controls`、`widgets`、`services`、`plugins` 与 `security`。配置只负责声明，运行期对象仍可通过 OpenLayers 原生 API 修改。

后续将提供：

- JSON Schema 与编辑器提示；
- 配置版本迁移；
- 环境变量与密钥占位；
- 远程配置加载与校验；
- 图层/控件/Widget 注册表。
