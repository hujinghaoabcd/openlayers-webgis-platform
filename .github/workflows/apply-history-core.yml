name: Apply history core

on:
  push:
    branches:
      - agent/history-core
    paths:
      - 'scripts/apply-history-core.py'
      - '.github/workflows/apply-history-core.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/history-core
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply-history-core.py
      - name: Commit history core integration
        shell: bash
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/core/src
          git commit -m "feat: integrate command history with map"
          git push origin HEAD:agent/history-core
